//! Placeholder for Fase 6B — the ratatui TUI.
//!
//! Currently re-exports the core lib so the workspace compiles. The
//! TUI itself (grid + vim-style navigation) lands in Fase 6B.
pub use lzt_wallcraft_core as core;
