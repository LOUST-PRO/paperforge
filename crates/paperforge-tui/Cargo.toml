[package]
name = "paperforge-tui"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "TUI for paperforge — grid + vim-style navigation over paperforge-core (placeholder, Fase 6B)"

[dependencies]
paperforge-core = { path = "../paperforge-core" }
# ratatui + crossterm planned for Fase 6B; keep this placeholder compiling
serde = { workspace = true }
serde_json = { workspace = true }
